/**
 * Helper to define an app.
 * In a TypeScript environment this would provide type safety.
 * @param {Object} appDefinition
 * @returns {Object}
 */
module.exports = function defineApp(appDefinition) {
  return appDefinition;
};
